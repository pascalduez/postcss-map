parser: babel-eslint
extends: standard
root: true
env:
  jest: true

rules:
  semi: [error, always]
  comma-dangle: [error, always-multiline]
  space-before-function-paren: [error, {
    anonymous: never,
    named: never,
    asyncArrow: always
  }]
